services:
  - type: web
    name: gleicop-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: bash backend/start.sh
    plan: free
    autoDeploy: true
    ...
